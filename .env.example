# Public mobile environment values only.
# Never place private API keys (OpenAI, Anthropic, Gemini, OCR, Supabase service-role,
# RevenueCat secret, or backend secrets) in Expo public variables or the mobile client.

EXPO_PUBLIC_APP_ENV=development
EXPO_PUBLIC_API_BASE_URL=https://example-api.local
