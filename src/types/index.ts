export interface Explanation {
  simpleExplanation: string;
  keyIdea: string;
  analogy: string;
  example: string;
  whyItMatters: string;
}

export interface TextBlock {
  id: string;
  text: string;
  index: number;
}
